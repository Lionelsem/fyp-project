import React from "react";

const FSMLayout = ({ children }) => {
  return (
    <div>
      <header>FSM Layout Header</header>
      <main>{children}</main>
    </div>
  );
};

export default FSMLayout;
